{
  "name": "@opennetwork/web-api-conversation-channel-state",
  "version": "4.2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --declaration && npm run tslint",
    "watch": "tsc -w",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {},
  "dependencies": {
    "io-ts": "^2.0.1",
    "schema-dts": "^0.4.2",
    "tsdef": "^0.0.13"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/opennetwork/web-api-conversation-channel-state.git"
  },
  "devDependencies": {
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
